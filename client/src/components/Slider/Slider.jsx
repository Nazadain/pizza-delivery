import { memo } from "react";
import "./Slider.css";

const Slider = memo(() => {
  return (
    <div className="slider">
      <div className="background__filter"></div>
    </div>
  );
});

export default Slider;
